name: Auto Post to WeChat Official Account

on:
  push:
    branches:
      - main
    paths:
      - "articles/*.md"
      - ".github/sync-platforms.json"
  workflow_dispatch:
    inputs:
      force:
        description: "投稿済み記事も強制更新する (--force)"
        type: boolean
        default: false
      article:
        description: "対象記事ファイル名 (例: 2026-03-13-claude-code-hooks.md)"
        type: string
        default: ""
      publish:
        description: "草稿を公開申請まで実行する (--publish)"
        type: boolean
        default: false

concurrency:
  group: post-to-wechat-${{ github.ref }}
  cancel-in-progress: false

jobs:
  post-to-wechat:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          token: ${{ secrets.GH_PAT }}

      - name: Fetch tracking data from tracking-data branch
        run: |
          if git fetch origin tracking-data 2>/dev/null; then
            git show origin/tracking-data:scripts/wechat-posted.json > scripts/wechat-posted.json 2>/dev/null || true
          fi

      - name: Check if WeChat posting is enabled
        id: config
        run: |
          ENABLED=$(jq -r '.platforms.wechat.enabled' .github/sync-platforms.json)
          echo "enabled=$ENABLED" >> "$GITHUB_OUTPUT"

      - name: Set up Python
        if: steps.config.outputs.enabled == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.config.outputs.enabled == 'true'
        run: pip install -r scripts/requirements.txt

      - name: Find newly added or modified articles
        if: steps.config.outputs.enabled == 'true'
        id: changed
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.article }}" ]; then
            CHANGED="articles/${{ inputs.article }}"
          else
            CHANGED=$(git diff --name-only --diff-filter=ACM HEAD~1 HEAD -- 'articles/*.md' | tr '\n' ' ')
          fi
          echo "files=$CHANGED" >> "$GITHUB_OUTPUT"
          echo "変更された記事: $CHANGED"

      - name: Post articles to WeChat Official Account
        if: steps.config.outputs.enabled == 'true' && steps.changed.outputs.files != ''
        env:
          DEEPL_API_KEY: ${{ secrets.DEEPL_API_KEY }}
          WECHAT_APP_ID: ${{ secrets.WECHAT_APP_ID }}
          WECHAT_APP_SECRET: ${{ secrets.WECHAT_APP_SECRET }}
          WECHAT_THUMB_MEDIA_ID: ${{ secrets.WECHAT_THUMB_MEDIA_ID }}
        run: |
          FORCE_FLAG=""
          PUBLISH_FLAG=""
          if [ "${{ inputs.force }}" = "true" ]; then
            FORCE_FLAG="--force"
          fi
          if [ "${{ inputs.publish }}" = "true" ]; then
            PUBLISH_FLAG="--publish"
          fi
          for file in ${{ steps.changed.outputs.files }}; do
            echo "投稿中: $file"
            python scripts/post-to-wechat.py $FORCE_FLAG $PUBLISH_FLAG "$file" || echo "警告: $file の投稿に失敗しました"
          done

      - name: Save tracking to tracking-data branch
        if: steps.config.outputs.enabled == 'true'
        run: |
          cp scripts/wechat-posted.json /tmp/tracking.json
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git ls-remote --exit-code origin tracking-data > /dev/null 2>&1; then
            git fetch origin tracking-data
            git checkout -b tracking-data-tmp origin/tracking-data
          else
            git checkout --orphan tracking-data-tmp
            git rm -rf . || true
          fi
          mkdir -p scripts
          cp /tmp/tracking.json scripts/wechat-posted.json
          git add scripts/wechat-posted.json
          if git diff --staged --quiet; then
            echo "追跡ファイルに変更なし"
          else
            git commit -m "chore: Update WeChat posting tracking"
            for i in 1 2 3 4 5; do
              if git ls-remote --exit-code origin tracking-data > /dev/null 2>&1; then
                git pull --rebase origin tracking-data 2>/dev/null || true
              fi
              git push origin tracking-data-tmp:tracking-data && break
              echo "push失敗、リトライ $i/5"
              sleep $((i * 3))
            done
          fi
